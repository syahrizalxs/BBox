const state = {
  user: {
    name: 'Syahrizal Setiawan',
    kelas: '12',
  },
}

const getters = {
  getUserInfo: (state) => state.user,
}

const actions = {}

const mutations = {}

export default {
  state,
  getters,
  actions,
  mutations,
}
